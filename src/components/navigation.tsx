import styled from "styled-components";

const Navigation = () => {
  return (
    <NavWrap>
      <div></div>
    </NavWrap>
  );
};

// Styles
const NavWrap = styled.div``;

// Exports
export default Navigation;
